# Copy to .env. Never commit the real key. Shell environment takes precedence.
DEEPSEEK_API_KEY=
# Persistent APP-WIDE conservative USD spending limit, not an account balance.
TOTAL_BUDGET_USD=8.00
# Optional. Default: ./data. Do not delete the ledger to reset a spent budget.
# MAFIA_DATA_DIR=/home/yourname/.local/share/cozy-mafia
